# Branch purpose

`experiment/ext-image-copy-probe` exists only to compile and runtime-test the newer foreign-toplevel image-copy capture path. It must remain separate from stable applet code until the runtime gate is passed.
